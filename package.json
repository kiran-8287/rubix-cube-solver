{
  "name": "rubiks-cube-solver",
  "version": "1.0.0",
  "description": "Interactive Rubik's Cube solver with Kociemba algorithm",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "predeploy": "vite build && node -e \"require('fs').copyFileSync('dist/index.html','dist/404.html')\"",
    "deploy": "gh-pages -d dist"
  },
  "dependencies": {
    "cubejs": "^1.1.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.0.0",
    "vite": "^7.2.6",
    "gh-pages": "^6.2.0"
  }
}
